{{!empty abierta_tmpl template}}

<div class="selection">
  <div class="form-group">
    <label for="options" class="col-sm-2 control-label">Respuesta correcta</label>
    <div class="col-sm-10">
      <input data-id="{{proposed_answer.0.id}}" class="form-control" name="is_correct-{{question_number}}" required value="{{proposed_answer.0.answer}}">
    </div>
  </div>
</div>
