{{!empty seleccionTmpl template}}

<div class="selection">
    <div class="form-group text-center">
      <div class="col-sm-1 col-md-offset-10 control-label text-center">Marcar la opción correcta</div>
      <div class="col-sm-1 control-label text-center">Puntaje para la respuesta</div>
    </div>
    <div class="form-group">
      <label for="options" class="col-sm-2 control-label">Opción 1</label>
      <div class="col-sm-8">
        <input data-id="{{proposed_answer.0.id}}" class="form-control" name="options" required value="{{proposed_answer.0.answer}}"/>
      </div>
      <div class="col-sm-1 text-center">
        <input type="checkbox" value="1" name="is_correct-{{question_number}}" {{#if proposed_answer.0.is_correct}}checked="checked"{{/if}}>
      </div>
      <div class="col-sm-1 text-center">
        <input type="number" class="form-control" name="score-{{question_number}}" required value="{{proposed_answer.0.score}}"/>
      </div>
    </div>
    <div class="form-group">
      <label for="options" class="col-sm-2 control-label">Opción 2</label>
      <div class="col-sm-8">
        <input data-id="{{proposed_answer.1.id}}" class="form-control" name="options" required value="{{proposed_answer.1.answer}}"/>
      </div>
      <div class="col-sm-1 text-center">
        <input type="checkbox" value="2" name="is_correct-{{question_number}}" {{#if proposed_answer.1.is_correct}}checked="checked"{{/if}}>
      </div>
      <div class="col-sm-1 text-center">
        <input type="number" class="form-control" name="score-{{question_number}}" required value="{{proposed_answer.1.score}}"/>
      </div>
    </div>
    <div class="form-group">
      <label for="options" class="col-sm-2 control-label">Opción 3</label>
      <div class="col-sm-8">
        <input data-id="{{proposed_answer.2.id}}"  class="form-control" name="options" required value="{{proposed_answer.2.answer}}"/>
      </div>
      <div class="col-sm-1 text-center">
        <input type="checkbox" value="3" name="is_correct-{{question_number}}" {{#if proposed_answer.2.is_correct}}checked="checked"{{/if}}>
      </div>
      <div class="col-sm-1 text-center">
        <input type="number" class="form-control" name="score-{{question_number}}" required value="{{proposed_answer.2.score}}"/>
      </div>
    </div>
    <div class="form-group">
      <label for="options" class="col-sm-2 control-label">Opción 4</label>
      <div class="col-sm-8">
        <input data-id="{{proposed_answer.3.id}}" class="form-control" name="options" required value="{{proposed_answer.3.answer}}"/>
      </div>
      <div class="col-sm-1 text-center">
        <input type="checkbox" value="4" name="is_correct-{{question_number}}" {{#if proposed_answer.3.is_correct}}checked="checked"{{/if}}>
      </div>
      <div class="col-sm-1 text-center">
        <input type="number" class="form-control" name="score-{{question_number}}" required value="{{proposed_answer.3.score}}"/>
      </div>
    </div>
  </div>
